/**
 * Legacy CRA stub. In the boilerplate, ABIs live in
 * packages/contracts (updated by packages/hardhat deploy export).
 * Kept for reference / migration parity.
 */
console.log(
  '[create_contracts] No-op: use `bun run deploy` from the monorepo root to refresh packages/contracts.',
)
