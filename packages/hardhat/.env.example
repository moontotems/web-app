# Local Hardhat RPC (used by localhost network)
LOCALHOST_RPC_URL=http://127.0.0.1:8545

# Deployer mnemonic (or use mnemonic.txt via `bun run generate`)
MNEMONIC=

# Mainnet deploy / verify
MAINNET_RPC_URL=
ETHERSCAN_API_KEY=

# Optional: override default network name
# HARDHAT_NETWORK=localhost
