import React from 'react'

import { CreaturesCarousel } from '../../../sharedComponents'

export default function CreatureDesktopView({ ethereumProps, nftAppProps }) {
  return (
    <CreaturesCarousel
      ethereumProps={ethereumProps}
      nftAppProps={nftAppProps}
    />
  )
}
