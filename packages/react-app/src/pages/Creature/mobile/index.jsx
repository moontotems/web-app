import React from 'react'
import { CreaturesCarousel } from '../../../sharedComponents'

export default function CreatureMobileView({ ethereumProps, nftAppProps }) {
  return (
    <CreaturesCarousel
      ethereumProps={ethereumProps}
      nftAppProps={nftAppProps}
    />
  )
}
